# Indoone Splash

Android launch splash uses the same Indoone brand mark as the launcher icon.

Reference: the main web `.brand-mark` is a 34px rounded square with a diagonal purple gradient (`#5E2CE2` to `#9B51F1`) and a bold white `I`. Android keeps that visual identity rather than introducing a new splash logo.
