name: Android Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.0'
      - name: Assemble debug APK
        run: gradle -p android assembleDebug --stacktrace
      - name: Verify APK output
        shell: bash
        run: |
          set -euo pipefail
          apk="$(find android/build/outputs/apk/debug -maxdepth 1 -type f -name '*.apk' -print -quit)"
          if [ -z "$apk" ]; then
            echo "No debug APK found under android/build/outputs/apk/debug"
            echo "APK files produced anywhere under android/build/outputs:"
            find android/build/outputs -type f -name '*.apk' -print || true
            exit 1
          fi
          echo "Found APK: $apk"
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: indoone-debug-apk
          path: android/build/outputs/apk/debug/*.apk
          if-no-files-found: error

  sync-main-to-develop:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Merge main into develop
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin develop
          git checkout -B develop origin/develop
          git merge --no-edit origin/main
          git push origin develop
